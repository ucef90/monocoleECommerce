{
  "name": "monancle-site",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "pg": "^8.16.3"
  },
  "scripts": {
    "start": "node backend/src/server.js",
    "start:legacy": "node server.js",
    "seed:products": "node backend/src/scripts/seed-products.js",
    "db:inspect": "sqlite3 backend/data/monocle.sqlite '.tables'",
    "docker:up": "docker compose up --build",
    "docker:up:pg": "docker compose -f docker-compose.postgres.yml up --build"
  }
}
